<Window x:Class="ZenBIM.Views.ManualReorderingOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ZenBIM Manual" 
        Height="330" Width="380"
        WindowStartupLocation="Manual" Topmost="True"
        ResizeMode="NoResize" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False">

    <Window.Resources>
        <DropShadowEffect x:Key="Shadow" BlurRadius="25" ShadowDepth="8" Opacity="0.25"/>
    </Window.Resources>

    <Border Background="White" CornerRadius="18" Margin="20" BorderBrush="#00C853" 
            BorderThickness="1" Effect="{StaticResource Shadow}" MouseDown="Window_MouseDown">
        <Grid Margin="25,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <TextBlock Text="Manual Mode" FontWeight="Bold" Foreground="#1F1F1F" FontSize="18" VerticalAlignment="Center"/>
                <Button Click="BtnClose_Click" Width="30" Height="30" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" Cursor="Hand" VerticalAlignment="Top">
                    <TextBlock Text="✕" Foreground="#999999" FontWeight="Bold" FontSize="16"/>
                </Button>
            </Grid>

            <TextBlock Grid.Row="1" x:Name="TxtStatus" Text="Select element..." FontSize="13" Foreground="#757575" Margin="0,10,0,0" HorizontalAlignment="Left" TextWrapping="Wrap"/>

            <StackPanel Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,15">
                <TextBlock Text="Next Value:" FontSize="12" Foreground="#BDBDBD" HorizontalAlignment="Center" Margin="0,0,0,5"/>

                <TextBlock x:Name="TxtNextValue" Text="XX.00" FontSize="42" FontWeight="Bold" Foreground="#00C853" HorizontalAlignment="Center" Margin="0,5"/>

                <TextBlock Text="(ESC for menu)" FontSize="11" Foreground="#E0E0E0" FontStyle="Italic" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>

            <Grid Grid.Row="3" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="BtnUndo" Grid.Column="0" Content="⎌ Undo" Click="BtnUndo_Click" Height="45" Background="#F5F5F5" BorderThickness="0" Cursor="Hand" IsEnabled="False" Foreground="#616161" FontSize="13" FontWeight="SemiBold">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button x:Name="BtnAction" Grid.Column="2" Content="Continue" Click="BtnAction_Click" Height="45" Background="#00C853" Foreground="White" BorderThickness="0" Cursor="Hand" FontWeight="Bold" IsEnabled="False" FontSize="13">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>